#include<stdio.h>

int main(){
  
for ( int i = 1; i <=10; i++){
i++;

printf("%d ",i);
}
printf("\nEnd of loop\n");
return 0;
}
/*
กำหนดตัวแปร i เป็น 1 ลูปทำงานเมื่อ i<=10 ค่าของ i เพิ่มขึ้นทีละ 1
ค่าของ i เพิ่มขึ้นทีละ 1
แสดงผลลัพธ์ค่าของ i
แสดงข้อความ End of loop
จบการทำงาน
*/